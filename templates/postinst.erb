#!/bin/bash

echo $0 $*

PACKAGE="<%= package %>"

if [ "$1" == "configure" ]; then
  cd /opt/$PACKAGE
  if [ -x configure ]; then
    echo "Running /opt/$PACKAGE/configure"
    ./configure $*
  fi

  if [ -f Gemfile ]; then
    bundle install --deployment
  fi
fi	

exit 0

