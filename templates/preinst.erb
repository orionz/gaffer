#!/bin/bash

echo $0 $*

PACKAGE="<%= package %>"

if [ "$1" == "install" ]; then
  echo adding user $PACKAGE
  useradd $PACKAGE --shell /bin/bash --home /opt/$PACKAGE --create-home
fi

#if [ "$1" == "upgrade" ]; then
#fi

exit 0
